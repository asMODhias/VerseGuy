<Window
    x:Class="VerseguY.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VerseguY.UI"
    Title="VerseguY" Height="720" Width="1280">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="#0A1628" Padding="8">
            <Button x:Name="OrgTabButton" Content="Organization" AutomationProperties.AutomationId="Tab:Organization" Margin="0,4" />
            <Button x:Name="AuthNavButton" Content="Auth" AutomationProperties.AutomationId="Nav:Auth" Margin="0,4" />
        </StackPanel>

        <Frame x:Name="MainFrame" Grid.Column="1" />
    </Grid>
</Window>