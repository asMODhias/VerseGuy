#!/usr/bin/env bash
set -euo pipefail

echo "Starting dev tasks..."
# Start master server in background (example)
# cargo run -p master_server &
# Start UI dev server (if available)
# (cd ui/web && npm start)

echo "Dev script: edit and uncomment the services you need to run locally."